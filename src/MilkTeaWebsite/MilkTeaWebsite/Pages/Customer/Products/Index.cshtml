@page
@model MilkTeaWebsite.Pages.Customer.Products.IndexModel
@{
    ViewData["Title"] = "Sản phẩm";
    Layout = "~/Pages/Shared/_CustomerLayout.cshtml";
}

<div class="container mt-4">
    <h2 class="mb-4">Sản phẩm của chúng tôi</h2>
    
    <!-- Category Filter -->
    <div class="row mb-4">
        <div class="col-md-12">
            <div class="btn-group" role="group">
                <a asp-page="/Customer/Products/Index" class="btn btn-outline-primary @(Model.SelectedCategoryId == null ? "active" : "")">
                    Tất cả
                </a>
                @foreach (var category in Model.Categories)
                {
                    <a asp-page="/Customer/Products/Index" asp-route-categoryId="@category.Id" 
                       class="btn btn-outline-primary @(Model.SelectedCategoryId == category.Id ? "active" : "")">
                        @category.CategoryName
                    </a>
                }
            </div>
        </div>
    </div>

    <!-- Products Grid -->
    @if (Model.Products != null && Model.Products.Any())
    {
        <div class="row">
            @foreach (var product in Model.Products)
            {
                <div class="col-md-3 col-sm-6 mb-4">
                    <div class="card product-card h-100">
                        <img src="@(string.IsNullOrEmpty(product.ImageUrl) ? "/images/no-image.jpg" : product.ImageUrl)" 
                             class="card-img-top" alt="@product.ProductName">
                        <div class="card-body">
                            <h5 class="card-title">@product.ProductName</h5>
                            <p class="card-text text-muted small">@product.Description</p>
                            <p class="text-primary fw-bold">@product.Price.ToString("N0") đ</p>
                            @if (product.IsAvailable && product.StockQuantity > 0)
                            {
                                <div class="d-grid gap-2">
                                    <a asp-page="/Customer/Products/Detail" asp-route-id="@product.Id" 
                                       class="btn btn-primary btn-sm">
                                        <i class="fas fa-eye"></i> Chi tiết
                                    </a>
                                </div>
                            }
                            else
                            {
                                <button class="btn btn-secondary btn-sm w-100" disabled>
                                    Hết hàng
                                </button>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="alert alert-info text-center">
            <i class="fas fa-info-circle"></i> Không có sản phẩm nào.
        </div>
    }
</div>
